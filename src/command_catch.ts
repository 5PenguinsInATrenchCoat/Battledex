import { State } from "./state.js";

export async function commandCatch(state: State, ...args: string[]) {
  const pokeName = args[0].toLowerCase()
  const pokemon = //continue here
    
  console.log("Throwing a Pokeball at ${pokeName}...");

  
}
