import type { State } from "./state.js";

  // Set game version, unsure currently what this will look like in REPL
  export async function commandSetgame (state: State, ...args: string[]) {
    console.log("Game version set to: ")
}
